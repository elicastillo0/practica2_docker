version: "3"
services:
  api:
    build: ./api
    ports:
      - "8080:3000"
    environment:
      - MONGO_URI=mongodb://db:27017/nba
    depends_on:
      - db

  db:
    image: elicastillo/mongodb-server-test
    ports:
      - "27017:27017"
